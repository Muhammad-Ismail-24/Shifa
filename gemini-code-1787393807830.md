# 🚀 Architecture Update — AI Pipeline (Ismail)

Hey Sufiyan, I just pushed a major refactor to the AI pipeline to make it significantly faster and immune to rate limits. Everything is scaffolded and ready, but you need to know about a few changes that affect how your API interacts with my orchestrator.

## 🛠️ What I Upgraded

1. **Fully Async Pipeline:** All my agents (`triage`, `extractor`, `identifier`, `composer`) are now asynchronous. This prevents the server from blocking during Gemini calls.
2. **Multi-Model LLM Router (`utils/llm_router.py`):** I built a custom fallback router. If `gemini-1.5-flash` hits a 429 Rate Limit error, the system automatically cascades down to `gemini-3.5-flash-lite`, `3.1-flash-lite`, etc. We won't crash from RPM limits during the demo.
3. **Parallel Hospital Search:** I optimized the execution flow in `orchestrator.py`. As soon as the Triage Agent says "proceed", your `places_search` tool fires concurrently in the background (`asyncio.create_task`) while Gemini is busy diagnosing. This shaves off a lot of response time.
4. **Urdu Normalization & Smart RAG:** Added a text normalizer to catch tricky Urdu spelling variations for emergency keywords, and upgraded the RAG chunker to split by paragraphs so we don't cut medical sentences in half.

## ⚠️ What YOU Need to Do (`main.py`)

Because of these upgrades, the `run_pipeline` function has changed slightly.

1. **AWAIT the Pipeline:** In your FastAPI `POST /analyze` route, you MUST `await` my function since it is now entirely async. 
   ```python
   # Do this:
   result = await run_pipeline(body.urdu_text, body.latitude, body.longitude, body.history)