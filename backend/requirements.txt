# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# AI — Gemini (inference + embeddings)
google-generativeai==0.7.2

# Vector store client
qdrant-client==1.9.1

# Settings / env loading
pydantic==2.7.1
pydantic-settings==2.3.1

# HTTP client (Places API calls)
httpx==0.27.0

# PDF parsing (WHO documents ingestion)
pypdf==4.2.0

# Database boilerplate (SQLite, not active Day 1)
sqlalchemy==2.0.31
