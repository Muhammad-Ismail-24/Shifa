# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation & settings
pydantic==2.7.1
pydantic-settings==2.3.1

# HTTP client (Places API calls)
httpx==0.27.0

# AI — Gemini (inference + embeddings)
google-generativeai==0.8.3
google-genai
# Text-to-speech — ElevenLabs (Urdu/English spoken replies)
elevenlabs>=2.0
# Vector store client
qdrant-client>=1.9.1

# PDF parsing (WHO documents ingestion)
pypdf==4.2.0

# Database boilerplate (SQLite)
sqlalchemy==2.0.31

# Supabase — cloud database + auth
supabase
PyJWT>=2.8.0
