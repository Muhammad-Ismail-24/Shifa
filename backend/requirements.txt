# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation & settings
pydantic==2.7.1
pydantic-settings==2.3.1

# HTTP client (Places API calls)
httpx==0.27.0

# AI — Gemini (inference + embeddings)
google-genai

# Vector store client
qdrant-client>=1.9.1

# PDF parsing (WHO documents ingestion)
pypdf==4.2.0

# Database boilerplate (SQLite)
sqlalchemy==2.0.31
